# AXF-121 — Non-resurrection invariant on restore

Implements the restore/rollback slice of AXF-9. The backup vendor and mechanism are `Deferred`
(G9-3, tracked in AXF-79), so this delivers the **invariant** and the integration point a restore
must respect — not a restore.

## Ratified sources

| Source                          | What it fixes                                                                                                                                                                                                                    |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-49 (`prd.md`)                | `exclusão, pseudonimização e eventual retenção aplicável dependem de política explícita e nunca reativam acesso revogado`; independent CSV (30 days) / documents / backup (90 days) windows are not decided locally.             |
| AD-18 (`ARCHITECTURE-SPINE.md`) | Recovery is an external, encrypted, segregated daily backup (RPO 24 h, RTO 8 h, 90-day retention, restore rehearsed quarterly). **Vendor still `Deferred`**; the system is not production-ready without a real approved restore. |
| AD-20                           | Closure has its own lifecycle; `CLOSED` is terminal; legal hold preserves content and never reactivates access.                                                                                                                  |
| AD-13 / G7-3                    | Native evidence over a custom `AuditEvent`; retention per domain via `AXF_CMT_RetentionPolicy`; a snapshot family is what has to last.                                                                                           |
| AD-21                           | Authorization covers every surface, including exports and futures.                                                                                                                                                               |
| AD-24                           | An unknown result blocks unsafe repetition rather than authorizing a retry.                                                                                                                                                      |
| AD-29                           | Contracts between layers are binding; no local story decides a deadline, a vendor or a fallback.                                                                                                                                 |
| G9-3 / G9-6 item 4              | Parity/recovery criteria and the open execution phase: _provider/mechanism of backup + approved real restore_.                                                                                                                   |

The story itself (`AXF-121`) states the scope: the vendor/mechanism is `Deferred`, this story
implements the invariant — **non-reactivation marking plus minimal evidence preserved** — and a
simulated-adapter test does not substitute for a real restore.

## Components

| Component                                                  | Role                                                                                                                      |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `AXF_OBJ_ClosureRun__c.AXF_CLR_TAL_NonReactivationMark__c` | The mark: written once, with the terminal state, and frozen afterwards by the closure's `CLOSED` immutability rule.       |
| `AXF_PS_GestorFinanceiro`                                  | Field permission for the mark, mirroring the other `AXF_CLR_TAL_*` fields: the closure writes the mark in `USER_MODE`.     |
| `ALT_CLS_RestoreInvariant`                                 | Builds the mark (write path) and answers, per restored scope, whether reactivation is permitted (read path, fail-closed). |
| `ALT_CLS_ClosureOrchestrator`                              | `markClosed()` writes the mark in the same update that sets `CLOSED` — the single terminal transition, so no path closes a run without its proof. |
| `ALT_CLS_RestoreInvariantTest`                             | Acceptance coverage for the AC, including the negative case.                                                              |

No new object, no new acronym: the mark reuses the closure run that already is the scope's private,
non-deletable, terminal-immutable lifecycle record. The mark's key set is what makes it safe to
keep: `schemaVersion`, `scopeKey`, `correlationId`, `policyVersion`, `closedAt`, `fingerprint`. The
digest is taken over exactly those keys (the fingerprint itself excluded), and the mark's `closedAt`
is the run's own completion instant, not a second clock reading.

Deployment order matters: the field is deployed **before** the permission set. Deploying both in one
operation leaves the field permission out of the permission set, and the closure then fails on
`AccessLevel.USER_MODE` with `Operation failed due to fields being inaccessible`.

## Acceptance criteria

1. **Given** a later restore or rollback, **when** deleted/pseudonymised data is found in a backup,
   **then** it is not reactivated in the active service.
   `assess(holders)` returns `NOT_REACTIVATED` for any scope with a known closure lifecycle, and the
   guard performs no reactivation itself: the restored source stays in custody and collection stays
   paused.
2. **And** minimal evidence preserves the proof of the lifecycle without reconstructing private
   content. The mark carries only the allowlisted keys, digested with SHA-256 over exactly those
   fields; the digest is what makes a spliced or altered mark detectable, and a key outside the
   allowlist is refused outright. The class never reads a content field, so nothing private can
   reach the proof.
3. **Fail-closed defaults (AD-24).** Refusal is the safe answer for a closure still running, for a
   `CLOSED` run whose mark was never written (any closure that reached `CLOSED` before AXF-121
   shipped), for a mark belonging to another scope, and for an altered mark. Only a scope with no
   closure lifecycle at all is allowed to reactivate.

## Authorization

The entry point requires the closure capability `AXF_CanCloseInstallation` (reused from
`ALT_CLS_ClosureOrchestrator.CP_CLOSE`, single source of truth) and throws `FORBIDDEN` otherwise.
The lifecycle read must not depend on the caller's record visibility: the invariant is a protection
duty, and a caller who cannot see a closure must never be answered "allowed". Record visibility is
governed by the class's sharing keyword, not by `WITH SYSTEM_MODE` (which exempts object and field
permissions only) — a `with sharing` guard returning `never closed` for an invisible run would be a
fail-open. The class is therefore `without sharing`, like the other privileged `ALT_CLS_*` guards,
and the query additionally reads `WITH SYSTEM_MODE`. Only content-free fields are read, and the
caller only ever learns about scopes it already named.

## Acceptance criteria → tests

| AC                                                   | Test                                                                        |
| ---------------------------------------------------- | --------------------------------------------------------------------------- |
| AC-1 a closed scope is not reactivated                | `restoredScopeOfAClosedClosureIsNotReactivated` (also asserts the restored source stays in custody and collection stays paused) |
| AC-2 minimal evidence, no private content, allowlist  | `markIsContentFreeAndTamperEvident`, `untrustedProofFailsClosed`            |
| AC-3 fail-closed defaults                             | `untrustedProofFailsClosed` (`MARK_MISSING`, spliced, smuggled, truncated, altered), `neverClosedScopeIsAllowedAndRunningClosureIsRefused` (in-progress closure) |
| Authorization and input contract                      | `accessAndInputAreRefused`, `capabilityWithoutRecordVisibilityStillRefuses` |

## Known boundary (stays open in AXF-79)

The mark lives inside the same database as the data it protects. A restore that also rolls back the
closure evidence is therefore outside the reach of this build; the deferred mechanism must guarantee
that the closure evidence outlives, or is reasserted after, the restored window. This is exactly why
a real restore rehearsal — not a simulated adapter — remains a production gate (AD-18, G9-6 item 4).

Three further limits belong to the same deferred decision, and are recorded rather than closed here:
nothing in the tree calls `assess()` yet, so the invariant is not enforced on the live surfaces that
can put a scope back in service (Pluggy resume, holder confirmation); a refused reactivation is not
recorded anywhere, so the attempt leaves no evidence of its own; and the digest is tamper-evident,
not tamper-proof — it is an unkeyed SHA-256 over public fields, so the load-bearing controls are the
`scopeKey` binding plus the `CLOSED` immutability rule, not the digest alone. `schemaVersion` is
matched exactly, so a future bump needs a version-aware reader or every older mark becomes a refusal.

## Validation

- **Executed in `AXON_DEV`** (`michel.carvalho.lopes@axon.com.dev`, org id `00Daj0000145rSHEAY`):
  - Deploy of the story delta: `Succeeded`, 0 component errors.
  - `ALT_CLS_RestoreInvariantTest`: **6 test methods, all passed** (test run `707aj00001FP6CQ`;
    the run summary reports `testsRan`/`passing` 7 and `failing` 0 because it also counts the
    `@TestSetup` entry).
  - `ALT_CLS_ClosureOrchestratorTest` (the suite of the class this story changed): **6 test methods,
    all passed** (test run `707aj00001FO6tL`, same summary convention, 0 failures).
  - The first executions failed and drove four corrections: the orchestrator's `load()` did not
    select `AXF_CLR_EXI_ScopeKey__c` (`SObjectException`), the field permission was missing because
    field and permission set were deployed together (`Operation failed due to fields being
    inaccessible`), `accessAndInputAreRefused` called `Test.startTest()` twice
    (`FinalException: Testing already started`), and `capabilityWithoutRecordVisibilityStillRefuses`
    proved the `SYSTEM_MODE`-only read was a fail-open (`REACTIVATION_ALLOWED` for a capability
    holder who cannot see the run), which is why the class is now `without sharing`.
- Local quality gate: prettier clean on the changed non-Apex files; the Apex prettier check could
  not run locally (`spawn EPERM` from the Apex plugin's JVM), so `ALT_CLS_*` formatting is unverified
  by that gate; Jest 31 suites / 237 tests; trigger-handler boundary 27 triggers; CI script tests
  13 + 11. No LWC, Aura or JS is changed by this story.
