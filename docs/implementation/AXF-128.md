# AXF-128 — Neutralização de contrapartidas no consolidado, replay e reversão

- **Jira:** https://axon-personal-finances.atlassian.net/browse/AXF-128 (parent AXF-1; split from AXF-5)
- **Branch:** `feature/AXF-128-neutralization` (base `origin/sprint/sprint-4` @ `91bf6db`).
- **Siblings already delivered and extended, not rebuilt:** AXF-126 (`propose`/`confirm`/`discard`,
  guard, integrity reads, controller) and AXF-127 (derived singular projection).

> Two rules only: (1) the counter-entries derived from a confirmed economic allocation are netted
> atomically **only** when the calculation receives every participant as an authorized holder — the
> consolidated view — while any narrower view keeps its own contribution; (2) the allocation's
> reversal creates compensating records and never edits the confirmation, with an equivalent replay
> returning the durable result and a divergent payload conflicting. No new object, field, permission
> set, Custom Label or metadata container.

---

## What the ratified contracts say, and where

| Contract           | Ratified content                                                                                                                                                                                                                                                                                                   | Where it was read                                                                                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `alloc-v2`         | `AllocationIdentity v2 = SHA-256(frame("alloc-v2", fatoIdentidade, targetType, targetIdentity, economicKind))`; `householdKey` removed, label had to move `alloc-v1 → alloc-v2`.                                                                                                                                   | `AXF-5-ECONOMIC-ALLOCATION-RECONCILIATION.md` §5 and §8 item 4 (`_bmad-output/planning-artifacts/architecture/architecture-Axon-Finance-2026-08-21/`).                                         |
| **G6**             | Gate 6 — "identidades e efeitos", closed 30/08/2026: every material mutation carries an **expected version + idempotency key**, divergence → `CONFLICT`, consistent lock order by type rank + Id, no universal `CommandExecution`; the same document is the one that names `alloc-v2` and the lock order in §5/§8. | `ARCHITECTURE-SPINE.md` §"Fechamento G6 — identidades e efeitos" (G6-1, G6-7) and §8 of the reconciliation above, which titles the identity/lock-order/version items "(G6)".                   |
| Lock order         | `active token(20) → fato de origem(30) → RA/FTX(50) → EAS/EA(60)`, by Id. Already implemented in `propose`/`confirm` (fact 30 before set 60); the reversal follows the same order.                                                                                                                                 | `AXF-5-ECONOMIC-ALLOCATION-RECONCILIATION.md` §5/§8 item 5; `ALT_CLS_EconomicAllocationService` class header.                                                                                  |
| `operationId`s     | Major fixed at `v1` for `economicallocation.set.confirm`, and **authorized**: `economicallocation.set.propose.v1`, `economicallocation.set.revise.v1`, `economicallocation.set.reverse.v1`.                                                                                                                        | Same document, §6 ("Operações ausentes") and §8 item 6.                                                                                                                                        |
| Reversal semantics | "reversão cria registros compensatórios sem editar a confirmação"; allocations are **always positive**; reversal is a **compensating record**, never an edit or delete; exact 100% at confirmation.                                                                                                                | §3 of the same document; `FR-50` in `prd.md` ("Contrapartidas internas conciliam visões individuais e são neutralizadas apenas na consolidação, sem expor detalhes privados"); `epics.md:204`. |

`economicallocation.set.reverse.v1` was therefore **not invented**: it is the ratified operationId for
this effect. `alloc-v2` framing is reused through `AXF_CLS_IdentityFraming.key(...)` — no local hashing.

## What it delivers

| Area       | Component                                                                                                                                                                                                               |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-1 rule  | `ALT_CLS_ConsolidationService.consolidate` — internal counter-entries derived from a confirmed allocation, netted per fact, only under a complete authorized perimeter.                                                 |
| AC-2 rule  | `ALT_CLS_EconomicAllocationService.reverse(ReverseInput)` — `economicallocation.set.reverse.v1`: compensating set + parcel, replay-safe, `CONFLICT` on divergence.                                                      |
| AC-2 guard | `ALT_CLS_EconomicAllocationGuard` — the `CONFIRMED → SUPERSEDED \| REVERSED` transition may release the single active token and clear the derived projection; neither field is movable while the set stays `CONFIRMED`. |

## AC-1 → implementation

**Derived internal counter-entries.** A confirmed allocation of a fact derives one counter-entry per
parcel, between the fact's holder and the allocated person/company. `FR-50` defines them by their
function: they "reconcile the individual views" of the two sides and are "neutralized only in the
consolidation".

**Only in the consolidated view, atomically.** Before counting, the calculation asks whether _every_
participant of that fact's allocation is present as a fully authorized holder (`fullScope`, i.e. the
same `UserRecordAccess.HasAllAccess` test that AXF-122 already applies to a holder's coverage). The
decision is taken once per fact, so a fact is never netted by halves:

- **Complete perimeter** → the counter-entries are neutralized: the fact contributes once, through its
  economic attribution (`ALLOCATION` parcels), and the material fact is not counted again. 300 split
  70/30 contributes 210 + 90 = 300, never 600.
- **Incomplete perimeter** (any participant outside the scope or with unverified coverage) → nothing is
  netted. The fact keeps its contribution where it materially is, on its holder (`HOLDER`), and every
  authorized participant **other than the holder** still contributes its own parcel. The holder's own
  parcel is skipped precisely because it is already inside that material contribution.

**Individual views preserve their redacted contribution.** With the reversal of the old unconditional
behaviour, a holder whose fact was allocated away no longer loses its own fact: the holder's own view
shows its contribution, and no participant outside the scope is named or counted — the existing
`ALLOCATED_OUTSIDE_SCOPE` / `ALLOCATED_TO_UNVERIFIED_HOLDER` exclusions and `DEGRADED` confidence are
reused unchanged, so a pending counter-entry is never presented as if it had been netted.

## AC-2 → implementation

**Reversal creates compensating records.** `reverse(setId, expectedVersion, operationKey)` writes, in
one savepointed transaction:

1. a **compensating set** — same fact identity, next revision, `SetKey = key(identity, revision)`,
   created `DRAFT` and then `CONFIRMED` so the guard's own transition rules apply, with a **single 100%
   parcel for the fact's own holder**, conserving magnitude and original currency. That is the parity
   rule confirmation itself applies (`confirm()`), applied mechanically — not a new rule;
2. its `AXF_EAS_TXT_OperationKey__c` = the **G6 identity of this effect**,
   `IdentityFraming.key([OP_REVERSE, operationKey])` — framed under its own operationId so it can never
   be confused with a raw `propose` key stored in the same field;
3. the end of the confirmation — `REVERSED` state, version + 1, and, in the _same_ DML, the release of
   the single active token (unique field, so the compensation can take it) and the clearing of the
   derived singular projection (AXF-127: it does not outlive the set's authority).

Once done, the compensating set is the only `CONFIRMED` set of that fact and holds the active token:
the fact keeps exactly one attribution authority, now back at its holder.

**The confirmation is never edited.** Every identity field of the reversed set (`FactIdentity`,
`SetKey`, anchors, holder, magnitude, currency, revision), both operation keys, and **every parcel byte
for byte** (`Id`, account, percent, magnitude, currency, redaction policy, `AllocationKey`) are captured
before and asserted identical after. Only state, optimistic version, active token and projection move.

**Replay and conflict.** An equivalent replay — same framed key, same expected version — returns the
durable compensation read back from the database and appends nothing (the set count of the fact stays
at two). A divergent payload never produces a second compensation: a different version under the same
key conflicts, a different key against an already reversed target conflicts, and a stale version on a
live confirmation conflicts before anything is written. A `DRAFT` is never reversed (`NOT_CONFIRMED`),
and capability is checked before any set is read (`FORBIDDEN`).

## Judgement calls and residuals

- **Two AXF-122 assertions were corrected, not weakened.** `confirmedAllocationSharesFollowAttribution`
  and `allocationSharesAreValidatedRoundedAndUnverifiedHoldersDistinguished` encoded the _unconditional_
  neutralisation (the holder's material fact dropped whenever a confirmed allocation existed, and a
  fact netted by halves when one participant was missing). Under the ratified rule the same fixtures now
  assert the corrected totals (400 → 600 and 333.35333334 → 400.02333334) **plus** the newly preserved
  holder contribution. No assertion was deleted or skipped; both files were verified green before and
  after on the same org.
- **Re-allocation after a reversal is a different operation.** The compensating set holds the active
  token, so `propose` refuses a new revision (`ACTIVE_SET_EXISTS`) exactly as it does for any confirmed
  set; re-allocating a fact is the sibling `economicallocation.set.revise.v1` (supersession), which no
  AXF-128 acceptance criterion requires and which is therefore **not implemented** here.
- **No reason/note field was added.** The ratified §5 field list has no reversal reason or "reverses"
  lookup, and the spec's "Ask First" list makes changing §5 a contract amendment. The compensating set
  is linked to what it compensates by its fact identity plus the next revision — the relation the guard
  itself already names ("reverse with a compensating set").
- **`AXF_EAS_TXT_OperationKey__c` widened in meaning.** Its description now reads
  `propose/confirm/reverse`, because that field is documented as "the idempotency key of the last
  applied operation" and the reversal stores its framed key there. The alternative — a dedicated field —
  would have changed the ratified §5 contract.
- **The `personHoldersAreAuthorizedAndNonAxonAccountsAreNotHolders` red is pre-existing.** It asserts
  `result.holders.size() == 1` for a scope holding one Axon person plus `new Account(Name = 'Conta
Comum')` created **without** an explicit record type. `AXON_DEV` has only two Account record types
  (`AXF_Person`, `AXF_Business`, queried in this session), so the default one applies, the account
  becomes a valid holder and the count is 2. Reproduced identically with the pristine
  `origin/sprint/sprint-4` classes deployed and run before this pass's changes (see Validation).

## Tests

| Class                                                                                                                                                    | Covers                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ALT_CLS_ConsolidationServiceTest.internalCounterEntriesAreNeutralizedOnlyInTheConsolidatedView`                                                         | AC-1: complete perimeter neutralizes and the fact counts once (300, `FULL`, zero exclusions); the holder's individual view keeps its own material fact without adding its own parcel on top, stays `DEGRADED`, names nobody and still reports the outside parcel.                                                                 |
| `ALT_CLS_ConsolidationServiceTest.confirmedAllocationSharesFollowAttribution` and `allocationSharesAreValidatedRoundedAndUnverifiedHoldersDistinguished` | Corrected to the ratified rule: a participant outside the authorized scope (or without verified coverage) leaves the counter-entry pending, so the holder keeps its material fact and each authorized participant keeps its own parcel.                                                                                           |
| `ALT_CLS_EconomicAllocationServiceTest.reversalCompensatesWithoutEditingTheConfirmationAndReplaysSafely`                                                 | AC-2: compensating records (next revision, one 100% parcel to the holder, conserving magnitude and currency), the confirmation edited in nothing (content fields and every parcel compared before/after), single active token, single `CONFIRMED` set, equivalent replay returns the durable compensation and does not duplicate. |
| `ALT_CLS_EconomicAllocationServiceTest.reversalRejectsDraftStaleVersionAndDivergentReplays`                                                              | AC-2 negatives: `NOT_CONFIRMED` for a `DRAFT`, `CONFLICT` for a divergent version under the same key, for another key against the reversed target and for a stale version on a live confirmation, `FORBIDDEN` without the capability, and the ended set frozen against content and state writes.                                  |

No LWC changed, so no Jest test was added.

## Validation

- Org: **`AXON_DEV` only** (`AXON_UAT`/`AXON_PROD` untouched). No `CustomLabels` and no `translations/`
  container was deployed; no label or translation changed.
- Deploy (`sf project deploy start -o AXON_DEV --wait 25 --json -m "ApexClass:…"` for the five classes
  plus `-m "CustomField:AXF_OBJ_EconomicAllocationSet__c.AXF_EAS_TXT_OperationKey__c"`): **Succeeded**,
  6/6 and then 5/5 components on the final content, 0 failures.
- Tests (`sf apex run test -o AXON_DEV --tests ALT_CLS_ConsolidationServiceTest --tests
ALT_CLS_EconomicAllocationServiceTest --test-level RunSpecifiedTests --code-coverage --result-format
json --wait 30`): `testsRan` **28**, `passing` **26**, `failing` **2**, `skipped` 0. The three tests
  this pass adds
  (`internalCounterEntriesAreNeutralizedOnlyInTheConsolidatedView`,
  `reversalCompensatesWithoutEditingTheConfirmationAndReplaysSafely`,
  `reversalRejectsDraftStaleVersionAndDivergentReplays`) pass, and so do the corrected AXF-122 tests
  and all AXF-126/AXF-127 tests. Coverage from that run: `ALT_CLS_ConsolidationService` 401/456 (88%),
  `ALT_CLS_EconomicAllocationService` 489/542 (90%), `ALT_CLS_EconomicAllocationGuard` 107/109 (98%),
  `ALT_CLS_EconomicAllocationIntegrity` 15/16 (94%), `AXF_CLS_CTRL_EconomicAllocation` 68/82 (83%),
  both triggers 6/6 (100%).
- The two failures are the pre-existing reds described above, both reproduced before this pass:
  `invalidSharesAreRejectedAtomicallyWithoutPartialPersistence` (AXF-126, DEV default record type
  `AXF_Business`) and `personHoldersAreAuthorizedAndNonAxonAccountsAreNotHolders` (same org-data cause;
  verified red with the pristine `origin/sprint/sprint-4` classes deployed and run in this session).
- Local CI gate (the five `quality` steps of `.github/workflows/salesforce-ci.yml`): `prettier --check`
  **exit 0** over the 6 changed files — run against the **committed LF content** (this checkout has
  `core.autocrlf=true`, so the working copies are CRLF while the blobs CI checks out are LF; the same
  command over the CRLF working copies flags all six files, including the four this pass barely touches
  for line endings only); `eslint` skipped (no changed Lightning `.js`); `npm run test:unit -- --
--runInBand` **exit 0**, 31 suites / 237 tests; `node scripts/ci/trigger-handler-boundary.mjs`
  **exit 0**, 27 triggers; `node --test scripts/ci/salesforce-delivery.test.mjs
scripts/ci/trigger-handler-boundary.test.mjs` **exit 0**, 19 tests.
