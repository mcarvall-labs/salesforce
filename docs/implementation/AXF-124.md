# AXF-124 — Recalcular e revalidar o consolidado em mudança de contexto/revogação

- **Jira:** https://axon-personal-finances.atlassian.net/browse/AXF-124 (parent AXF-1; split from AXF-4)
- **Branch:** `feature/AXF-124-consolidated-revalidation` (base `origin/sprint/sprint-4` @ `b9ae3f7`).
- **Siblings already delivered and consumed, not rebuilt:** AXF-122 (`ALT_CLS_ConsolidationService`,
  consolidated totals over the authorized scope), AXF-128 (counter-entry neutralization), AXF-3/AXF-8
  (authorized context + authorization revalidation announcement), AXF-137/138 (horizon/scenario
  engine and its policy application), AXF-143/144 (confidence panel and its canonical states).

> **Diff of this pass: two LWC controllers' JavaScript (consumers of the consolidated view), their
> Jest suites, one Apex test method, and this document. No new object, field, permission set,
> Custom Label, message channel or metadata container. No production Apex class changed.**

---

## Step 0 — what already existed, and what was missing (with evidence)

The acceptance criteria were split by surface and each half was checked against the code that is
already merged, before anything was written.

| AC half                                                                       | State before this pass                                                                                                                                    | Evidence                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (a) server recomputes KPIs + confidence + explanations as one coherent unit    | **Already delivered.** Each consolidated entry point derives its whole result from the current effective access in a single call: no cached result, no incremental sum, no retained state. | `ALT_CLS_ConsolidationService.consolidate` reads the requested set, coverage (`UserRecordAccess.HasAllAccess`), sources and facts per call, `WITH USER_MODE` (`ALT_CLS_ConsolidationService.cls:165-196`, `:240-284`) and derives confidence last from the reasons collected while deriving the same result (`:413-420`).                                        |
| (a) a narrower/different authorized set removes the previous contribution      | **Already delivered and already pinned.**                                                                                                                  | `ALT_CLS_ConsolidationServiceTest.partialAccessIsUnknownAndDegradesWithoutMisleadingTotal` (`:445-516`): the withheld holder's amount never appears, the total is the reduced one and never the inflated one. `ALT_CLS_ConfidencePanelServiceTest.unauthorizedHolderIsNotConfirmedAndEmptyScopeBlocks` (`:502-544`): level, reasons, allowed actions, source rows and `listHolders()` all move with the set, with no trace of the unauthorized holder. |
| (a) previous context data does not remain **on screen, in reachable cache or accessible announcements** | **MISSING.** The publisher existed; **no consumer subscribed.** | The event is published on every context selection (`aXF_LWC_contextBar.js:100-113`) and on authorization revalidation (`:119-143`, `changeReason: "AUTHORIZATION_REVALIDATION"` at `:134`), and the channel's own description is the contract: *"Announces an authorized holder context selection. **Consumers must clear stale state and refetch.**"* (`AXF_ContextChanged.messageChannel-meta.xml:3`). A repository-wide search for `AXF_ContextChanged__c` matched the message channel metadata, `aXF_LWC_contextBar` and that component's test — nothing else; `subscribe` from `lightning/messageService` was imported by no component. Both reachable consolidated surfaces therefore kept the previous context's result and its `aria-live` announcement until the user acted again. |
| (a) reachable cache                                                           | **MISSING.** The holder list is cached client-side and was never refreshed on revalidation.                                                                | `AXF_CLS_CTRL_ConfidencePanel.getHolders` is `@AuraEnabled(cacheable=true)` (`:7`) and `AXF_CLS_CTRL_ForecastComparison.getContext` is `@AuraEnabled(cacheable=true)` (`:18`); each component wired it once and never refreshed it, so a revoked holder stayed reachable in the picker.                                                                                |
| (b) the next response revalidates effective access through the approved native model and removes the contribution without remaining legitimate access | **Already delivered.** Account sharing is the authority (AXF-3) and every read is `with sharing` + `WITH USER_MODE`; a holder whose access is no longer legitimate is not loaded and its contribution is withheld or excluded. | `ALT_CLS_ConsolidationService.cls:11-20` (the access contract), `:243-268` (`HasAllAccess` false ⇒ `UNKNOWN`/`UNVERIFIED`, contributions withheld), `:229-238` (not visible ⇒ `NOT_AUTHORIZED`, nothing loaded). Pinned by the two tests cited above and by `ALT_CLS_ForecastComparisonServiceTest.partialAccessDegradesStaleSchedulesBlockAndGuardsHold` (`:587+`, `COVERAGE_UNVERIFIED` exclusion). |
| (b) "**on the next response or event**"                                       | **MISSING — this is the whole gap.** There was no event path at all: the revalidation existed only if the user changed something himself.                    | Same evidence as (a): no subscriber, so no response was ever re-requested on the event.                                                                                                                                                                                                                                                                          |
| (b) invalidates any derived result and does not preserve the previous total as an apparently current fallback | **MISSING on the client.** The server never returns a previous total (proven above), but the client kept rendering it, which is precisely the fallback the AC forbids. | `aXF_LWC_confidencePanel.js:353-378` and `aXF_LWC_forecastComparison.js:330-363` (pre-change): `this.panel` / `this.result` were only cleared by the component's own inputs.                                                                                                                                                                                                 |

**Conclusion.** The server half of both criteria was already delivered and already falsifiable by
existing tests, so nothing was rebuilt there — only one new Apex test was added (see below) to pin
the two properties that were not yet asserted as a sequence. The genuinely missing half was the
event-driven invalidation and revalidation on the reachable consolidated surfaces, which is the
ratified contract of the existing message channel.

## What it delivers

| Area                       | Component                                                                                                                                                                                                                     |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (a)+(b) invalidation       | `aXF_LWC_confidencePanel` and `aXF_LWC_forecastComparison` subscribe to `AXF_ContextChanged__c` in `connectedCallback` and unsubscribe in `disconnectedCallback`.                                                              |
| (a)+(b) no stale result    | On the event both components discard the derived result (`panel` / `result`) **before** anything else, clear the `aria-live` announcement, and bump `requestToken` so a response that started before the event can never land after it. |
| (a) reachable cache        | Both refresh the cached holder wire with `refreshApex`, instead of trusting it — a holder whose access was revoked leaves the picker.                                                                                          |
| (b) revalidation           | When a scope is still selected the server is asked again through the same entry points; nothing is decided in the browser. The confidence panel on a record page defers to the refreshed wire, which reloads it, so the event costs one server call and not two. |
| (b) removal, not fallback  | The revalidated answer replaces the whole unit; the previous total is never rendered as if it were current.                                                                                                                    |

## Ratified contracts located and followed

| Contract                                     | Content                                                                                                                                                                     | Where it was read                                                                              |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Context change announcement                  | "Consumers must clear stale state and refetch." The payload carries `accountId`, `contextType`, `label`, `presentationCurrency`, `selectionVersion`, `changeReason`.        | `force-app/main/default/messageChannels/AXF_ContextChanged.messageChannel-meta.xml:3`, `:19`, `:22`. |
| Consumer duty on a context change (AXF-3)    | "Cada seleção leva `selectionVersion`; consumidores devem limpar estado anterior e recarregar dados, atualidade e confiança."                                                 | `docs/implementation/AXF-3.md` (AC3 row "Propagação").                                          |
| Authorization revalidation (AXF-8)           | Publishes `AUTHORIZATION_REVALIDATION` "so any consumer **discards derived state before refetching**"; the bar clears its own selection and revalidates on the secure `WITH USER_MODE` query. | `docs/implementation/AXF-8.md` §1.                                                             |
| Account sharing is the authority (AXF-3)     | No parallel grant model; the effective access is decided by the native model (OWD + sharing + `USER_MODE`).                                                                  | `docs/implementation/AXF-3.md` (row "Autoridade"); `AXF_CLS_AuthorizedContextService.cls:1-4`.   |
| The confidence panel renders, never derives  | The level and the allowed actions come from the service; the UI only renders them, and the gate vocabulary is consumed from the policy that declares it.                    | `ALT_CLS_ConfidencePanelService.cls:14-30`; `AXF_CLS_CTRL_ConfidencePanel.cls:1-5`.             |
| Foreach result is display-only               | The comparison never authorizes a material action; the client may not invent one.                                                                                          | `ALT_CLS_ForecastComparisonService.cls:18-22`.                                                  |

## Judgement calls

- **Any context change invalidates, not only the ones that name the same scope.** Both components
  have their own scope input, and neither is bound to the context bar's selection. Binding them
  would be a contract nobody ratified; treating the announcement as what the channel says it is — a
  reason to clear and refetch — is the ratified reading and the only one that satisfies "data from
  the previous context does not remain". A re-derivation of an unrelated scope returns the same
  answer, so nothing is lost by revalidating.
- **No new server "revalidate" method.** Revalidation is the existing `explain` / `compare` / (for
  AXF-122) `consolidate` call: they read effective access on every invocation. A parallel method
  would be new machinery for a behavior that is already proven by test.
- **The announcement is cleared, not replaced.** No new Custom Label was created and no
  `translations/` container was deployed: the pendencies record that deploying the translation
  container on the shared `AXON_DEV` can wipe another line of work's `AXF_AccessLevel_*` labels, and
  the loading state of both components already announces itself (`lightning-spinner` alternative
  text inside a `role="status"` region in the confidence panel). Clearing is what the AC requires;
  inventing a new string was not required.
- **`requestToken` is bumped on the event.** The existing token already rejected superseded
  responses; without bumping it, a request issued before the event would land after it and re-render
  the previous context.
- **Unsubscribe in `disconnectedCallback`,** following the listener discipline of
  `aXF_LWC_contextBar.js:47-54`, so a navigation does not leave a live subscription behind.

## AXF-32 is still open — what that restricts

AXF-32 ("Confirmar uma transferência interna") is **not delivered**: `AXF_OBJ_InternalTransfer__c`
and its neutrality key do not exist in this branch. It is a predecessor listed by the story, and
the pendency **P2** asked whether it is a real prerequisite.

**It is not.** Neither acceptance criterion here mentions internal transfers, and this pass adds no
contribution, allocation or normalization rule of its own: it discards a derived result and asks the
existing servers to derive it again. Nothing in the delta reads, composes or assumes the internal
composition of a fact's contribution, so it cannot be affected by whether a transfer pair is
recognized. AXF-135 already refuses `TRANSFER`/`REFUND` link roles without writing, and the
neutralization of a transfer pair is declared as AXF-32's own contract by AXF-129 — that stays
untouched. **What it restricts:** none of the two criteria above; the delivery is complete without
AXF-32. What it defers is only that, once AXF-32 lands, a transfer pair's neutralization will be
recomputed by this same discard-and-re-derive path, which needs no change here.

## Tests

| Class / file                                                                   | Covers                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ALT_CLS_ConsolidationServiceTest.contextChangeAndRevocationRecomputeTheWholeUnitWithoutKeepingThePreviousTotal` (new) | (a)+(b): one response derives KPIs, confidence and explanations together; a narrower authorized set returns 200 where the previous context returned 1499, with no contribution, holder or id of the previous context left in the answer; re-entering the first context derives 1499 again from scratch; a partially authorized holder is withheld with `COVERAGE_UNVERIFIED` while the same call degrades the confidence; after the team-member link is deleted, the next response to the **same** scope revalidates, removes the contribution and reports `NOT_AUTHORIZED` for it while the previous total is not re-presented. |
| `aXF_LWC_confidencePanel.test.js` — "discards the previous context on a change and revalidates it on the server" (new)  | (a)+(b): between the event and the server's answer the previous panel is off screen, the `aria-live` announcement is empty, the cached holder wire was refreshed and the server was asked again; the revalidated answer replaces the whole unit.                                              |
| `aXF_LWC_forecastComparison.test.js` — same name (new)                          | Same, on the KPI surface: the previous period/horizon tables and the confidence badge are gone before the answer arrives, the announcer is empty, `refreshApex` and a second `compare` are observed, and the revalidated `BLOCKED` unit replaces the previous totals.                          |
| `aXF_LWC_confidencePanel.test.js` — "revalidates a fixed record scope through the refreshed holder wire" (new) | (a)+(b) on a record page: the panel is cleared and the announcement emptied without a self-issued server call, and the refreshed holder wire re-applies the record scope and asks the server again.                                                                                          |
| `ALT_CLS_ConsolidationServiceTest.partialAccessIsUnknownAndDegradesWithoutMisleadingTotal` (`:445`) | Pre-existing; the removal of a contribution without remaining legitimate access, and the total not being the inflated one. Cited as evidence, not modified. |
| `ALT_CLS_ConfidencePanelServiceTest.unauthorizedHolderIsNotConfirmedAndEmptyScopeBlocks` (`:502`)  | Pre-existing; coherence of level + reasons + allowed actions + holder list with the set. Cited as evidence, not modified.                                    |

### Falsifiability of the new tests

- The three Jest tests were **killed by mutation**: replacing the body of `handleContextChanged` with
  `return;` in both components makes exactly those three fail (`3 failed, 12 passed` of 15) and
  restoring the implementation makes them pass (`15 passed`). They therefore detect the absence of
  the behavior, not merely its presence. The record-scope test carries a positive control (the panel
  really is rendered before the event), so it cannot pass vacuously.
- The Apex test is discriminating by construction: `narrow` (200) against the previous `wide` (1499),
  and `again` (1499) after `narrow`, so a `Result` that kept the previous total — or that replayed
  it as a fallback — fails the assertions instead of passing them.

## Validation

- Org: **`AXON_DEV` only**; `AXON_UAT` / `AXON_PROD` untouched. No label, translation, permission
  set, profile or object metadata was deployed.
- Deploy of the delta (`sf project deploy start -o AXON_DEV --wait 30 --source-dir
  force-app/main/default/lwc/aXF_LWC_confidencePanel --source-dir
  force-app/main/default/lwc/aXF_LWC_forecastComparison --source-dir
  force-app/main/default/classes/ALT_CLS_ConsolidationServiceTest.cls`): **Succeeded**, 3/3
  components, 0 errors.
- Tests (`sf apex run test -o AXON_DEV --tests ALT_CLS_ConsolidationServiceTest --tests
  ALT_CLS_ConfidencePanelServiceTest --tests ALT_CLS_ForecastComparisonServiceTest --test-level
  RunSpecifiedTests --wait 30`): **29 ran, 29 passing, 0 failing, 0 skipped**. The new method passes.
- **One org-drift finding, reported rather than hidden.** The first run of the same command (after
  deploying only the delta) reported **29 ran / 25 passing / 4 failing**:
  `ALT_CLS_ConsolidationServiceTest.internalCounterEntriesAreNeutralizedOnlyInTheConsolidatedView`
  (expected `HOLDER`, actual `ALLOCATION`),
  `...allocationSharesAreValidatedRoundedAndUnverifiedHoldersDistinguished` (expected `400.02333334`,
  actual `333.35334`) and `...confirmedAllocationSharesFollowAttribution`
  (`NullPointerException`) — i.e. the AXF-128 neutralization behaviour was **absent from the
  deployed `ALT_CLS_ConsolidationService`** in the shared org — plus
  `ALT_CLS_ForecastComparisonServiceTest.partialAccessDegradesStaleSchedulesBlockAndGuardsHold`
  (the known pendency P8). `ALT_CLS_ConsolidationService.cls` is **byte-identical** to
  `origin/sprint/sprint-4` on this branch (`git diff --stat origin/sprint/sprint-4..HEAD` over that
  path is empty), so the drift was in the org, not in the branch: deploying the branch's own
  unmodified class and re-running produced the 29/29 above. No test assertion was changed to reach
  it.
- Local CI gate (5 checks) — see the test summary artifact for the literal results.

## Limitations and declared gaps

1. **The presentation-currency change has no cross-component event.** AC (a) names "moeda". A change
   of the *holder context* carries `presentationCurrency` and is therefore covered, and a scenario or
   horizon change inside the comparison component already re-derives. But a change made in
   `aXF_LWC_reportCurrencyPreference` publishes nothing: `AXF_ContextChanged` is the only message
   channel in the repository and that component does not use it. Covering that would need a new or
   extended message channel — metadata whose form is not ratified — so **nothing was invented**;
   the gap is declared and needs a decision.
2. **The AXF-122 consolidated KPI surface has no runtime surface.** `ALT_CLS_ConsolidationService`
   is reachable only from Apex and tests (no controller, no LWC — presentation is AXF-125), so its
   part of both criteria is proven by Apex test and not by a browser surface.
3. **Provider-level consent revocation still contributes.** The confidence panel reports
   `CONSENT_REVOKED` / `CONSENT_STALE` as an exception on the row but keeps the source included, and
   the consolidation totals read sources by holder without consulting the connection's consent
   state. Read literally, AC (b) — "remove the contribution without remaining legitimate access" —
   is not satisfied at the provider level. That is the scope of **AXF-120** ("Tratar estados
   bloqueados e revogação externa Pluggy", not delivered), and nothing here changes it.
4. **No visual verification.** There is no rendered-page check; the client behaviour is proven by
   Jest only.
